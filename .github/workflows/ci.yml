name: CI Pipeline

on:
  push:
    branches: [ "main", "dev", "prod", "feature/**" ]
    tags: [ "v*" ]
  pull_request:
    branches: [ "main", "dev", "prod" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Tests
        run: |
          echo "Teams: Replace this with your own test commands!"
          echo "If these fail, Infra will NOT build the image."
          exit 0

; Suppress deprecated assert.active warning (PHP 8.3+)
assert.active = 0

; Never output PHP errors/warnings directly in HTTP responses
display_errors = Off
display_startup_errors = Off

; Keep logging errors server-side
log_errors = On

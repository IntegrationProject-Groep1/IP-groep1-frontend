services:
  rabbitmq_sender.client:
    class: Drupal\rabbitmq_sender\RabbitMQClient
    arguments:
      - '%env(RABBITMQ_HOST)%'
      - '%env(int:RABBITMQ_PORT)%'
      - '%env(RABBITMQ_USER)%'
      - '%env(RABBITMQ_PASS)%'
      - '%env(RABBITMQ_VHOST)%'